// Fires when the document is loaded
$(document).ready(function() {
	// Gets the context (has to fetch the DOM node from the jQuery object)
	var ctx = $("#animation").get(0).getContext("2d");
	// Start the path
});
